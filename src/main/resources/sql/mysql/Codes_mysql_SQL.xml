<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="codes">

    <select id="selectGroupList" resultType="codesVO">
        /* 코드 그룹 목록 조회 codes.selectGroupList */
        SELECT
            CODE_GRP_ID
            , CODE_GRP_NM
            , DESCRIPTION
            , USE_YN
            , CREATE_DT
        FROM
            TB_CODE_GRP
        WHERE 1=1
        <if test="useYn != null and useYn != ''">
            AND USE_YN = #{useYn}
        </if>
        ORDER BY CODE_GRP_ID
    </select>

</mapper>
