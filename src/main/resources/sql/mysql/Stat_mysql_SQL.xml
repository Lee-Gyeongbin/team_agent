<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ai.chatbot.stat">
    <!-- TODO 추후 시연 완료 후 삭제 -->
    <select id="selectStatList" resultType="chatbotVO">
        /* 통계 목록 조회 ai.chatbot.selectStatList */
        SELECT
            A.STAT_ID
            , A.STAT_NM
            , B.DETAIL_ITEM_CODE_NM AS DETAIL_ITEM_CD
        FROM
            STAT_ATTR A
        LEFT OUTER JOIN STAT_DETAIL_ITEM B
            ON B.DETAIL_ITEM_GRP_ID = A.STAT_DETAIL_ITEM_GRP_ID
        WHERE 1=1
            AND A.USE_AT = 'Y'
        GROUP BY A.STAT_ID

    </select>

</mapper>
